name: Tests

on:
  workflow_call:
    inputs:
      php-version:
        type: string
        default: '8.4'
      database:
        description: 'mysql, or sqlite to use the database configured in phpunit.xml'
        type: string
        default: 'mysql'
      shards:
        description: 'JSON array of shard numbers, e.g. [1, 2, 3, 4]'
        type: string
        default: '[1]'
      node-version-file:
        type: string
        default: '.nvmrc'
    secrets:
      COMPOSER_AUTH:
        required: false

permissions: {}

jobs:
  tests:
    name: Tests (Shard ${{ matrix.shard }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(inputs.shards) }}

    services:
      mysql:
        image: ${{ inputs.database == 'mysql' && 'mysql:8.4' || '' }}
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: testing
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ inputs.php-version }}
          coverage: none

      - name: Install composer dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          composer-options: '--prefer-dist'
        env:
          COMPOSER_AUTH: ${{ secrets.COMPOSER_AUTH }}

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ${{ inputs.node-version-file }}
          cache: npm

      - name: Laravel setup
        run: |
          cp .env.example .env
          php artisan key:generate
          npm ci
          npm run build

      - name: Configure MySQL
        if: inputs.database == 'mysql'
        env:
          DB_PORT: ${{ job.services.mysql.ports[3306] }}
        run: |
          {
            echo "DB_CONNECTION=mysql"
            echo "DB_HOST=127.0.0.1"
            echo "DB_PORT=${DB_PORT}"
            echo "DB_DATABASE=testing"
            echo "DB_USERNAME=root"
            echo "DB_PASSWORD=root"
          } >> "$GITHUB_ENV"

      - name: Execute tests
        run: vendor/bin/pest --parallel --shard="${SHARD}/${SHARD_TOTAL}"
        env:
          SHARD: ${{ matrix.shard }}
          SHARD_TOTAL: ${{ strategy.job-total }}
